// src/firebase/config.js — Deprecated (Shifted completely to Neon PostgreSQL)
// All auth and storage is now managed directly by the FastAPI backend & Neon DB.
export const auth = null;
export const db = null;
export default null;